<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zdjf.dao.app.appRelease">

<sql id="columnsAs">
		t1.id,t1.release_version,t1.sub_version, t1.release_content, t1.is_release, t1.down_url, t1.release_channel, t1.create_time,
		 t1.re_release_time
	</sql>

	<select id="selectAppReleasePage" resultType="com.zdjf.domain.app.AppRelease">
		select
		<include refid="columnsAs"/>
		from
		zd_app_release t1
		where 1=1
		<include refid="conds" />
		order by id desc
		limit #{firstrownum}, #{limit}
	</select>
	
	<select id="selectAppReleaseCount" resultType="java.lang.Long">
		select
		count(id)
		from zd_app_release
		where 1=1
		<include refid="conds" />
	</select>


</mapper>